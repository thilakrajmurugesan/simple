#include <stdio.h>
#include<ctype.h>
int main()
{
    int i=0;
    char str[]="c++";
    while(str[i])
    {
         if(alpha(str[i])
          printf("character c is an alphabetic\n", str[i] );
       else
        printf("character c is not an alphabetic\n",str[i]);
           }
    return 0;
}
